apiVersion: v1
kind: Service
metadata:
  name: metric-simulator
  labels:
    app: metric-simulator
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: metrics
  selector:
    app: metric-simulator
