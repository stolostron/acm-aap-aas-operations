apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-empty-pvc
  namespace: managed-services
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: gp2
