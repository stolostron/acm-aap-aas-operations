apiVersion: v1
kind: Secret
metadata:
  name: argocd-vault-plugin-credentials
  namespace: argocd
type: Opaque
stringData:
  VAULT_ADDR: <VAULT_ADDRESS>
  VAULT_TOKEN: <VAULT_TOKEN>
  AVP_TYPE: vault
  AVP_AUTH_TYPE: token
