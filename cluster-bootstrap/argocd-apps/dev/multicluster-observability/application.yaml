apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: multicluster-observability
  namespace: openshift-gitops
spec:
  destination:
    name: in-cluster
  project: default
  source:
    path: cluster-bootstrap/multicluster-observability/overlay/dev-thanos-aws
    repoURL: https://github.com/stolostron/acm-aap-aas-operations.git
    targetRevision: main
    plugin:
      name: argocd-vault-plugin-kustomize
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
